// Configuration
export const CONFIG = {
    // TMDB API KEY - User will need to replace this if the demo key hits limits
    // Using a temporary public demo key pattern or instructing user to replace it.
    // For now I will leave a placeholder that clearly indicates where to put it.
    TMDB_API_KEY: 'YOUR_TMDB_API_KEY_HERE', 
    TMDB_BASE_URL: 'https://api.themoviedb.org/3',
    IMAGE_BASE_URL: 'https://image.tmdb.org/t/p/original',
    POSTER_BASE_URL: 'https://image.tmdb.org/t/p/w500'
};

// Check if key is missing and alert user (logic will be in app.js)
